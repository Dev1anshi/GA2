name: My Simple Action

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 22f3003104@ds.study.iitm.ac.in
      - run: echo "Hello, World!"
       
